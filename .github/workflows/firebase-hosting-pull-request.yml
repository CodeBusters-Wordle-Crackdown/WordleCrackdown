# This file was auto-generated by thepermissions:
  checks: write
  contents: read
  pull- Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PRrequests: write
jobs:
  build_and_preview:
    if: ${{ github.event.pull_request.head.repo.full_name ==
on: pull_request
permissions:
  checks: write
  github.repository }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - contents: read
  pull-requests: write
jobs:
  build_and run: |
          cd CodeBustersWeb
          npm ci_preview:
    if: ${{ github.event.pull_request.head
          npm run build
      - uses: FirebaseExtended/action.repo.full_name == github.repository }}
    runs-on-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseService: ubuntu-latest
    steps:
      - uses: actions/Account: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_WORD_Bcheckout@v4
      - run: |
          cd CodeBustersWebUSTER_99508 }}
          projectId: word-buster-99508
